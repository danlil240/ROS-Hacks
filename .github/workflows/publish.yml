# DISABLED: APT repository moved to separate ros-hacks-apt repository
# This workflow has been moved to the ros-hacks-apt repository
# 
# Original workflow published APT repository to GitHub Pages
# Now handled by: https://github.com/danlil240/ros-hacks-apt

name: Disabled APT Workflow
on: 
  workflow_dispatch:
    inputs:
      note:
        description: 'APT publishing moved to ros-hacks-apt repository'
        required: false
        default: 'This workflow is disabled'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo "APT repository publishing has been moved to:"
          echo "https://github.com/danlil240/ros-hacks-apt"
          echo ""
          echo "The ros-hacks repository now only contains source code."
